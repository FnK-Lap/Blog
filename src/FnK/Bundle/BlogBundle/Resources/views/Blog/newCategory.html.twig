{% extends 'FnKBlogBundle::Layout/layout.html.twig' %}

{% block title %} - Nouvelle categorie{% endblock %}

{% block content %}
    {{form_errors(form)}}
    {{form_start(form, {'action': path('blog_category_new'), 'method': 'POST'})}}
        {{form_widget(form)}}
        <input type="submit" value='Enregistrer'>
    {{form_end(form)}}

{% endblock %}